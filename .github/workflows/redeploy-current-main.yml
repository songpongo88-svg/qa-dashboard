name: Redeploy Current Main

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/redeploy-current-main.yml'

permissions:
  contents: write

jobs:
  verify-and-redeploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Repair evaluation timestamp source parity
        run: |
          node scripts/patch-evaluation-last-updated-v87.mjs
          node scripts/patch-evaluation-last-updated-v88-safety.mjs
          node scripts/patch-evaluation-last-updated-v89-layout.mjs
      - name: Prepare exact final source state
        run: |
          rm .github/workflows/redeploy-current-main.yml
          node scripts/check-user-guide.mjs --record --reason "Repaired evaluation timestamp source parity and reviewed Case Detail appeal layout before production redeploy" --no-content-change
          git diff --binary > /tmp/redeploy-final.patch
      - name: Verify production build
        run: |
          npm ci
          npm run build
      - name: Commit only verified source changes
        run: |
          git reset --hard HEAD
          git apply /tmp/redeploy-final.patch
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Repair production build and redeploy appeal layout"
          git push
